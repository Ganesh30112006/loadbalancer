# Production environment configuration
# Replace these values with your actual production settings

# Backend API URL (relative path when frontend and backend are served from same domain)
VITE_API_URL=/api/v1
